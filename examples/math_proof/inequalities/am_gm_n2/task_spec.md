# goal
Prove the two-variable AM-GM inequality.

# milestones
- Propose a route to show (a + b)/2 >= sqrt(ab) for nonnegative a and b
- Validate at least one intermediate claim

# success_criteria
- Produce at least one validated finding that advances the proof

# constraints
- Strict JSON-only agent responses
- Use elementary algebra only

# conjecture
For nonnegative real numbers a and b, (a + b)/2 >= sqrt(ab).

# known_lemmas
- Squares are nonnegative
